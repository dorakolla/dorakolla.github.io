{
    "version": 2,
    "builds": [
      { "src": "/(.*)", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/$1" }
    ],
    "env": {
      "PORT": "3000",
        "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
      }
  }
  